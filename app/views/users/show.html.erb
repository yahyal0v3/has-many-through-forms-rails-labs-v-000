<h1> All <%= @user.username %> Comments</h1>

<ul>
  <% @user.comments.each do |comment| %>
    <li><%= comment.content %><br>
      <%= raise comment.inspect %>
    <%= link_to comment.post.title, post_path(comment.post) %></li>
  <% end %>
</ul>
